import './styling.css';

function Banner() {
    return(
        <div>
            <header>Orbit Report</header>
            <p>Click the buttons to see the satellite in that orbit type</p>
        </div>
    );
};

export default Banner;